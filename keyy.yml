---
 - hosts: dev
   user: ansible
   become: yes
   connection: ssh
   tasks:
           - name: copy ssh-key to remote

             authorized_key:
                     user: ansible
                     state: present
                     key: "{{lookup('file','/home/ansible/.ssh/id_rsa.pub')}}"
                   
